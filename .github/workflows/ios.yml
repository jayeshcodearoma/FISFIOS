name: iOS Build

on:
  workflow_dispatch:

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: read

jobs:
  build:
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install Dependencies
        run: npm install

      - name: Install CocoaPods
        run: |
          cd ios
          pod install

      - name: Get Team ID from Apple ID
        run: |
          # Free Apple ID thi Team ID automatically detect thase
          TEAM_ID=$(xcodebuild -showBuildSettings -workspace ios/FISFIOS.xcworkspace -scheme FISFIOS 2>/dev/null | grep DEVELOPMENT_TEAM | awk '{print $3}')
          echo "TEAM_ID=$TEAM_ID" >> $GITHUB_ENV
          echo "Team ID found: $TEAM_ID"

      - name: Build Archive
        run: |
          mkdir -p build
          xcodebuild archive \
            -workspace ios/FISFIOS.xcworkspace \
            -scheme FISFIOS \
            -configuration Release \
            -archivePath build/FISFIOS.xcarchive \
            DEVELOPMENT_TEAM="${{ env.TEAM_ID }}" \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            PROVISIONING_PROFILE_SPECIFIER=""

      - name: Create Export Options Plist
        run: |
          cat > exportOptions.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>development</string>
            <key>signingStyle</key>
            <string>automatic</string>
            <key>compileBitcode</key>
            <false/>
            <key>teamID</key>
            <string>${{ env.TEAM_ID }}</string>
          </dict>
          </plist>
          EOF

      - name: Export IPA
        run: |
          xcodebuild -exportArchive \
            -archivePath build/FISFIOS.xcarchive \
            -exportPath build/FISFIOS.ipa \
            -exportOptionsPlist exportOptions.plist \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO

      - name: Upload IPA
        uses: actions/upload-artifact@v4
        with:
          name: ios-ipa
          path: build/FISFIOS.ipa